// 1. Import react 💡
import "./Card.css";

export default function Card() {
  /* 2.1 Make use of the 'children' property inside of the 'prop' object
        So that you can make this component accept other components/Nodes and render them inside.
        For instance {props.children}

     2.2 Make use of the 'card-wrapper' css class at the outermost component. 
        @hint remember that classes in JSX are denoted by the className attribute   
   */
}
