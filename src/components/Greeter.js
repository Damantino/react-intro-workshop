// 1. Import react 💡

function Greeter() {
  /**
   *  2.1 Fill-in the greeter component so that it is able to receive a
   *  `name` prop and render a text node to the screen with it.
   *  
   *  2.2 Next, add a new prop called `emoji` and add it new you your name prop when
   *  rendering so that you end up showing something like this:
   *  Hello <Name> <emoji>
   * 
   * @hint Remember that props are passed as one single parameter into the Component 
   * through the `prop` Object
   */
}

// 3. Export me 💡