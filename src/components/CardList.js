// 1. Import react 💡

export default function CardList(props) {
  /**
   * 2.1 Create a new component that receives a `list` prop.
   *    This list prop should be an array of Objects with the following shape:
   *    { name: "John", lastName: "Doe" }
   *
   * 2.2 Iterate throught all the items in the list, and for each render a new Card component
   *     From the previous iteration.
   *
   * 2.3 Add a condition that checks if the list is empty. If it is, render a 'No users!' text
   */
}
